torch>=1.12.0
torchvision>=0.13.0
clip-by-openai
ftfy>=6.1.1
regex
tqdm>=4.64.0
Pillow
# Optional for loading NVIDIA .pkl StyleGAN2-ADA checkpoints:
# clone https://github.com/NVlabs/stylegan2-ada-pytorch and ensure its
# `legacy.py` and `dnnlib` are on PYTHONPATH
# Optional for loading NVIDIA .pkl StyleGAN2-ADA checkpoints:
# clone https://github.com/NVlabs/stylegan2-ada-pytorch and ensure its
# `legacy.py` and `dnnlib` are on PYTHONPATH